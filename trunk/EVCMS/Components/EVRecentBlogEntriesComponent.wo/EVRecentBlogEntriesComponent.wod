Link : WOHyperlink {
  directActionName = "blogEntry";
  actionClass = "Web";
  disabled = "~!dynamicBindings.blogEntry.content";
  ?entryId = dynamicBindings.blogEntry.primaryKey;
  ?pageId = context.page.page.primaryKey;
}
PageBlogRecentEntries : WORepetition {
  list = page.blog.recentEntries; // valid
  item = dynamicBindings.blogEntry;
}
